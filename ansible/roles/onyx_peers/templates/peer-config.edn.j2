{:onyx/id "{{ onyx_deployment_id }}"
 :zookeeper/address "{{ groups.zookeeper | join(',') }}"
 :onyx.messaging.aeron/offer-idle-strategy "{{ onyx_aeron_offer_idle_strategy }}"
 :onyx.messaging.aeron/poll-idle-strategy "{{ onyx_aeron_poll_idle_strategy }}"
 :onyx.messaging.aeron/embedded-driver? "{{ onyx_aeron_embedded_driver }}"
 :onyx.messaging.aeron/subscriber-count "{{ onyx_aeron_n_subscribers }}"
 :onyx.messaging/allow-short-circuit? "{{ onyx_aeron_short_circuit }}"
 :onyx.peer/job-scheduler "{{ onyx_job_scheduler }}"
 :onyx.messaging/peer-port "{{ onyx_peer_port }}"
 :onyx.messaging.aeron/write-buffer-size "{{ onyx_aeron_write_buffer_size }}"
 :onyx.peer/join-failure-back-off "{{ onyx_peer_join_backoff }}"
 :onyx.messaging/impl :aeron}
